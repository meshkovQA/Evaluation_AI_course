# RAG System API

Простая и мощная система RAG (Retrieval-Augmented Generation) для загрузки документов и получения AI-ответов на основе их содержимого.

## 🎯 Что это такое?

RAG система позволяет:
- Загружать документы разных форматов
- Автоматически создавать векторные индексы
- Задавать вопросы и получать ответы на основе содержимого документов
- Видеть источники информации в ответах

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <your-repo>
cd rag-system
cp .env.example .env
```

### 2. Запуск
```bash
docker-compose up --build
```

### 3. Проверка работы
```bash
curl "http://localhost:8002/health"
```

### 4. Открыть документацию
http://localhost:8002/docs

## 📁 Поддерживаемые форматы

| Формат | Расширения | Примеры |
|--------|------------|---------|
| PDF | `.pdf` | Отчеты, презентации |
| Word | `.docx` | Документы, инструкции |
| Текст | `.txt`, `.md` | Заметки, README |
| Таблицы | `.csv`, `.xlsx` | Данные, списки |
| Веб | `.html` | Страницы, статьи |
| Данные | `.json` | Конфигурации, API ответы |

## 🔧 Настройка

### Минимальная конфигурация (.env)
```env
# Векторная база (работает без API ключей)
VECTOR_DB_TYPE=chroma

# Embeddings - бесплатные модели HuggingFace
EMBEDDING_PROVIDER=huggingface
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Для полной функциональности добавьте OpenAI ключ
# OPENAI_API_KEY=sk-your-key-here
# DEFAULT_LLM_PROVIDER=openai
# EMBEDDING_PROVIDER=openai
```

### Продвинутая конфигурация
```env
# Размеры чанков
DEFAULT_CHUNK_SIZE=1000
DEFAULT_CHUNK_OVERLAP=200

# RAG параметры
MAX_RELEVANT_CHUNKS=5
SIMILARITY_THRESHOLD=0.7

# Ограничения
MAX_FILE_SIZE=52428800  # 50MB
```

## 📚 Использование API

### Загрузка документа
```bash
curl -X POST "http://localhost:8002/api/v1/documents/upload" \
  -F "file=@document.pdf" \
  -F "title=Мой документ"
```

### Вопрос к документам
```bash
curl -X POST "http://localhost:8002/api/v1/chat/" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "О чем этот документ?",
    "max_relevant_chunks": 3
  }'
```

### Список документов
```bash
curl "http://localhost:8000/api/v1/documents/"
```

### Поиск документов
```bash
curl "http://localhost:8002/api/v1/documents/search/?query=важная%20информация"
```

## 🐍 Python клиент

```python
import requests

class RAGClient:
    def __init__(self, base_url="http://localhost:8002"):
        self.api_url = f"{base_url}/api/v1"
    
    def upload_document(self, file_path, title=None):
        with open(file_path, 'rb') as f:
            files = {'file': f}
            data = {'title': title or 'Документ'}
            return requests.post(
                f"{self.api_url}/documents/upload",
                files=files, data=data
            ).json()
    
    def ask_question(self, question, max_chunks=3):
        payload = {
            "message": question,
            "max_relevant_chunks": max_chunks
        }
        return requests.post(
            f"{self.api_url}/chat/",
            json=payload
        ).json()
    
    def get_documents(self):
        return requests.get(
            f"{self.api_url}/documents/"
        ).json()

# Использование
client = RAGClient()

# Загрузка
result = client.upload_document('report.pdf', 'Отчет Q3')
print(f"Загружен: {result['document_id']}")

# Вопрос
answer = client.ask_question('Каковы основные результаты?')
print(f"Ответ: {answer['content']}")
print(f"Источников: {len(answer['sources'])}")
```

## 🔍 Основные endpoints

### Система
- `GET /` - Информация о системе
- `GET /health` - Проверка работоспособности
- `GET /info` - Детали конфигурации

### Документы
- `POST /api/v1/documents/upload` - Загрузка документа
- `GET /api/v1/documents/` - Список документов
- `GET /api/v1/documents/{id}` - Информация о документе
- `DELETE /api/v1/documents/{id}` - Удаление документа
- `GET /api/v1/documents/search/` - Поиск документов

### Чат
- `POST /api/v1/chat/` - Вопрос к документам

## 🔧 Локальная разработка

### Требования
- Python 3.11+
- Docker (опционально)

### Установка
```bash
# Виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Зависимости
pip install -r requirements.txt

# Запуск
uvicorn app.main:app --reload
```

## 📊 Мониторинг

### Проверка здоровья
```bash
curl "http://localhost:8002/health"
```

### Логи Docker
```bash
docker-compose logs -f rag-api
```

### Тестирование
```bash
python test_rag_system.py
```

## 🏗️ Архитектура

```
📁 Загрузка документа
    ↓
🔍 Парсинг содержимого (PDF, DOCX, TXT...)
    ↓
✂️ Разделение на чанки
    ↓
🧠 Создание embeddings
    ↓
💾 Сохранение в векторную БД
    ↓
❓ Вопрос пользователя
    ↓
🔍 Поиск похожих чанков
    ↓
🤖 Генерация ответа LLM
    ↓
📋 Ответ с источниками
```

## ⚙️ Компоненты

- **FastAPI** - REST API
- **ChromaDB** - векторная база данных
- **HuggingFace/OpenAI** - embeddings и LLM
- **PyPDF2, python-docx** - парсинг документов
- **Docker** - контейнеризация

## 🚨 Устранение неполадок

### Система не запускается
```bash
# Проверить логи
docker-compose logs rag-api

# Пересобрать
docker-compose down
docker-compose up --build
```

### Ошибки с API ключами
- Для базовой работы API ключи не нужны
- Используйте HuggingFace модели (бесплатно)
- OpenAI ключ нужен только для полной функциональности

### Проблемы с загрузкой файлов
- Максимальный размер: 50MB
- Поддерживаемые форматы: PDF, DOCX, TXT, CSV, JSON, HTML, MD, XLSX
- Проверьте права доступа к папке `storage/`

## 📋 Примеры ответов

### Успешная загрузка документа
```json
{
  "document_id": "doc-123",
  "filename": "report.pdf",
  "status": "processing",
  "message": "Документ успешно загружен и обрабатывается"
}
```

### Ответ чата с источниками
```json
{
  "content": "Согласно документу, продажи выросли на 15%...",
  "sources": [
    {
      "document_title": "Отчет Q3",
      "similarity": 0.89,
      "content": "Продажи в Q3 составили 150 млн руб, рост 15%..."
    }
  ],
  "metadata": {
    "sources_count": 1,
    "context_length": 850
  }
}
```

## 🔒 Безопасность

- Проверка типов файлов
- Ограничения размера загрузки
- Безопасные имена файлов
- Изоляция в Docker контейнере

## 🌟 Возможности расширения

- [ ] Аутентификация пользователей
- [ ] Поддержка изображений в PDF
- [ ] Интеграция с базами данных
- [ ] Кэширование ответов
- [ ] Метрики и аналитика

## 📞 Поддержка

- **Документация**: http://localhost:8002/docs
- **Проверка здоровья**: http://localhost:8002/health
- **Тестирование**: `python test_rag_system.py`

## 📄 Лицензия

MIT License - используйте свободно в своих проектах.