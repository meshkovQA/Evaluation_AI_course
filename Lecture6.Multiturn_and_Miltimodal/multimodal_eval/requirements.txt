# Мультимодальная оценка изображений
# Multimodal Image Evaluation
#
# ВАЖНО: этот блок несовместим с deepeval 4.x, потому что в 4.0
# были удалены MLLMTestCase, DeepEvalBaseMLLM и мультимодальные
# метрики (MultimodalAnswerRelevancyMetric и др.). Поэтому
# multimodal_eval нужно ставить в ОТДЕЛЬНЫЙ venv, а не в общий
# для остальных лекций (там deepeval==4.0.7).
#
# Пример:
#   cd Lecture6.Multiturn_and_Miltimodal/multimodal_eval
#   python3 -m venv .venv-multimodal && source .venv-multimodal/bin/activate
#   pip install -r requirements.txt

# PyTorch (CPU-only версия для экономии места)
torch>=2.0.0
torchvision>=0.15.0

# Метрики изображений
torchmetrics>=1.0.0
lpips>=0.1.4

# Transformers для CLIP
transformers>=4.30.0

# DeepEval для мультимодальных метрик.
# Пин на 3.x — в 4.0 удалены MLLMTestCase / DeepEvalBaseMLLM /
# MultimodalAnswerRelevancyMetric, на которых держится этот блок.
deepeval>=3.0,<4.0

# Работа с изображениями
Pillow>=9.0.0

# Окружение
python-dotenv>=1.0.0

# OpenAI клиент (для DeepEval прокси)
openai>=1.0.0
